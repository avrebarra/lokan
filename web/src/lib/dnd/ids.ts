// vendored from kan — https://github.com/kan/kan (AGPLv3)
// source: kan/apps/web/src/views/board/dnd/ids.ts — see NOTICE.md
export function getListBodyId(listPublicId: string): string {
  return `list-body:${listPublicId}`
}
